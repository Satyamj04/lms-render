# Copy this file to .env and fill values for your Postgres database and Django settings
DB_NAME=lms
DB_USER=postgres
DB_PASSWORD=admin@123
DB_HOST=localhost
DB_PORT=5432
SECRET_KEY=replace-with-a-secure-secret
DEBUG=True
# Optional: set to false for production
